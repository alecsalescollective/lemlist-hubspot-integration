# Supabase Configuration
SUPABASE_URL=https://xjeapcqrvlujeujpqqet.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# HubSpot Configuration
HUBSPOT_ACCESS_TOKEN=pat-na1-xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
HUBSPOT_PORTAL_ID=12345678

# Lemlist Configuration
LEMLIST_API_KEY=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Optional per-owner campaign overrides (used by API lead pipeline routing)
# If unset, API falls back to ids in api/config/routing.json
LEMLIST_CAMPAIGN_ALEC=
LEMLIST_CAMPAIGN_JANAE=
LEMLIST_CAMPAIGN_KATE=

# Polling Configuration
POLLING_INTERVAL_MS=300000

# Retry Configuration
RETRY_MAX_ATTEMPTS=3
RETRY_BASE_DELAY_MS=1000

# Logging
LOG_LEVEL=info
NODE_ENV=development

# Deployment Date (only process leads created after this date)
# Format: ISO 8601 (e.g., 2026-02-03T00:00:00.000Z)
DEPLOYMENT_DATE=2026-02-03T00:00:00.000Z

# Email Alert Configuration
ALERT_EMAIL_TO=alerts@yourcompany.com
ALERT_EMAIL_FROM=hubspot-lemlist-integration@yourcompany.com
SMTP_HOST=smtp.yourprovider.com
SMTP_PORT=587
SMTP_USER=your-smtp-username
SMTP_PASS=your-smtp-password
SMTP_SECURE=false

# Alert Threshold (send alert after this many consecutive failures)
ALERT_FAILURE_THRESHOLD=3
